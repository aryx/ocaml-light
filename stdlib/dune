(library
 (name stdlib)
 (wrapped false)
 ; -nopervasives -nostdlib
 (flags (:standard -unsafe-string))
 (modules
   set map
   stack sort
   lexing parsing
 )

 (libraries

  )
 
 (preprocess (pps ppx_deriving.show))
)
